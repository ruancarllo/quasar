{
  "dependencies": {
    "ts-node": "latest",
    "fs-extra": "latest",
    "preact": "latest",
    "sass": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/fs-extra": "latest",
    "@types/node": "latest"
  },
  "scripts": {
    "serve": "ts-node worker.ts serve",
    "build": "ts-node worker.ts build"
  }
}
